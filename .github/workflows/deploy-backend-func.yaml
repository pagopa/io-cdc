name: Deploy Function App (backend-func)

on:
  workflow_dispatch: {}
  push:
    branches:
      - master
    paths:
      - "apps/backend-func/CHANGELOG.md"
      - ".github/workflows/deploy-backend-func.yaml"

jobs:
  deploy:
    name: Deploy Backend Func
    uses: pagopa/dx/.github/workflows/release-azure-appsvc.yaml@5ee71138780439aa0af1c422e445468c77f5112c
    secrets: inherit
    with:
      workspace_name: backend-func
      environment: app-prod
      resource_group_name: io-p-itn-cdc-rg-01
      web_app_name: io-p-itn-cdc-backend-func-01
      use_labels: false
      disable_auto_staging_deploy: true
    permissions:
      attestations: write
      contents: read
      id-token: write
